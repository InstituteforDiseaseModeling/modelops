# Archive

Legacy design plans and proposals live here for reference (Azure job observability MVP, bundle workflow state flow, OCI integration plans, etc.). These docs predate the current architecture and should be treated as historical context only.
